When coding, include comments only to explain a tricky piece of code.

When committing, do not write a body but try to sum up the changes in one line.

Use `pixi` to run tests and linting.

Before commiting, be sure that `pixi run full-ci` passes.